# No API key needed - uses Claude Code CLI for planning

# Server port
BACKEND_PORT=3001

# Domain allowlist for navigation (comma-separated)
DOMAIN_ALLOWLIST=localhost,127.0.0.1

# Max steps before agent stops
MAX_STEPS=40

# Playwright headless mode (set to true for headless)
PLAYWRIGHT_HEADLESS=false
